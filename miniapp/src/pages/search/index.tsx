import { View, Text } from "@tarojs/components";

export default function SearchPage() {
  return (
    <View style={{ padding: "48px", textAlign: "center" }}>
      <Text style={{ color: "#94a3b8" }}>搜索页面（预留）</Text>
    </View>
  );
}
